(include_subdirs unqualified)

(library
 (name chess)
 (public_name ocamlchess)
 (libraries core_kernel core_kernel.univ monads)
 (ocamlopt_flags -O3)
 (foreign_stubs
  (language c)
  (names int64_stubs)
  (flags -mpopcnt))
 (preprocess (pps ppx_compare
                  ppx_fields_conv
                  ppx_hash
                  ppx_sexp_conv)))
